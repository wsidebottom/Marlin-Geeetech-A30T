* Fix "Serial.end()" hack in MarlinCore.cpp#setup() (currently needed for X axis to work)
* finish implementing LCD code handling
* extract temp etc. variables for L2 as data gathering takes too long during send...
    * update independently...